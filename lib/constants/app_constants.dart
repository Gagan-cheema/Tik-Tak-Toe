import 'package:flutter/material.dart';

// Constants for styling
class AppConstants {
  static const Color boardColor = Color(0xFF1A1A1A);
  static const Color xColor = Color.fromARGB(255, 255, 2, 2);
  static const Color oColor = Color.fromARGB(255, 0, 99, 247);
  static const Color glowColor = Colors.white54;
  static const Color tileColor = Colors.grey;
  static const Color backgroundColor = Colors.black;
  static const double baseTileSize = 100.0;
  static const double baseFontSize = 16.0;
  static const double basePadding = 16.0;
  static const double baseButtonHeight = 48.0;
}
